{
  "name": "landscape-vision",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20.19.0"
  },
  "scripts": {
    "frames": "node scripts/extract-frames.mjs",
    "dev": "node scripts/extract-frames.mjs && vite",
    "build": "node scripts/extract-frames.mjs && vite build",
    "preview": "vite preview",
    "test": "node --test tests/*.test.mjs"
  },
  "dependencies": {
    "gsap": "^3.15.0",
    "lenis": "^1.3.26"
  },
  "devDependencies": {
    "@playwright/test": "^1.63.0",
    "vite": "^6.4.3"
  }
}
